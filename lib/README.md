# KeyShare — lib

Core modules for key or credential sharing logic live here.

## Planned modules (examples)

- `vault/` — encrypted storage interface
- `share/` — time-limited share links
- `audit/` — access log schema

Add implementation when the product scope is defined.
