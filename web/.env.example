# PostgreSQL connection string (Neon, Supabase, or Railway free tier)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Secret for signing session cookies (generate a long random string)
JWT_SECRET="replace-with-a-long-random-secret"

# Optional: first admin account auto-assigned on signup
ADMIN_EMAIL="you@example.com"
